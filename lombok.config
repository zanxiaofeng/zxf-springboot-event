config.stopBubbling = true
# Allow @Qualifier to be placed on fields and copied onto @RequiredArgsConstructor
# constructor parameters, so multiple EventBus beans can be constructor-injected.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
